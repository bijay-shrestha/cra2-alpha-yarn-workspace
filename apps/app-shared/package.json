{
  "name": "@company/app-shared",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@company/core": "0.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "lint": "eslint src/**/*.js",
    "format": "prettier --write src/**/*.{js,jsx,json,css}",
    "analyze": "yarn run build -- --stats && webpack-bundle-analyzer build/bundle-stats.json"
  },
  "devDependencies": {
  },
  "eslintConfig": {
    "extends": "react-app"
  }
}
